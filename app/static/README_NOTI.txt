Este proyecto usa un archivo de audio para notificaciones en `static/noti.ogg`.
Coloca un archivo de sonido corto (ogg/mp3) en `app/static/noti.ogg` para que el navegador reproduzca sonido cuando lleguen nuevas notificaciones.
Si no deseas sonido, elimina la referencia al elemento <audio> en `plantilla.html` o deja el archivo vacío.
